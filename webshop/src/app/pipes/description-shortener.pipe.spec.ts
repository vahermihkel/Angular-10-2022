import { DescriptionShortenerPipe } from './description-shortener.pipe';

describe('DescriptionShortenerPipe', () => {
  it('create an instance', () => {
    const pipe = new DescriptionShortenerPipe();
    expect(pipe).toBeTruthy();
  });
});
